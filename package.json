{
  "name": "adios",
  "version": "1.0.0",
  "description": "",
  "main": "build.js",
  "scripts": {
    "start": "node ./build && php -S localhost:8080",
    "start2": "php -S localhost:8080",
    "deploy": "timestamp=$(date \"+%s\") && node ./build.js && commit $timestamp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fwd/server": "github:fwd/server"
  }
}
